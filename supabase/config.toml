project_id = "tzyattbwtvadcjvrirlh"


[functions.process-email-queue]
verify_jwt = true

[functions.public-invoice]
verify_jwt = false

# Safaricom cannot send an Authorization header to the Daraja callback URL.
[functions.mpesa-callback]
verify_jwt = false

# The POS invokes this function with the signed-in user's JWT. It is the only
# STK Push entry point used by the browser.
[functions.mpesa-stk-push]
# Authentication is enforced inside the function so browser CORS preflight
# requests can receive the required headers before a JWT is checked.
verify_jwt = false

[functions.migrate-helper]
verify_jwt = false
